# ════════════════════════════════════════════════════════════════════════════
# INGRESS - event-demo-1
# Generated by Opsera Code-to-Cloud Enterprise v0.903
# RULE 104: NO TLS section - TLS handled at Ingress Controller level
# RULE 105: BOTH ingressClassName AND annotation required for NGINX compatibility
# ════════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: event-demo-1
  labels:
    app: event-demo-1
    tenant: opsera
  annotations:
    # RULE 105: Required for older NGINX Ingress Controller versions
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    # NO cert-manager.io/* annotations (RULE 104)
spec:
  # RULE 105: Required for Kubernetes v1.18+
  ingressClassName: nginx
  # NO spec.tls section (RULE 104)
  rules:
    - host: PLACEHOLDER_SUBDOMAIN
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: event-demo-1
                port:
                  number: 80
