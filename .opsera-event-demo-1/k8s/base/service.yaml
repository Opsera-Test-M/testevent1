# ════════════════════════════════════════════════════════════════════════════
# SERVICE - event-demo-1
# Generated by Opsera Code-to-Cloud Enterprise v0.903
# ════════════════════════════════════════════════════════════════════════════
apiVersion: v1
kind: Service
metadata:
  name: event-demo-1
  labels:
    app: event-demo-1
    tenant: opsera
spec:
  # RULE 23: Services MUST be type ClusterIP (NOT LoadBalancer)
  # Shared NGINX Ingress Controller handles external traffic
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: event-demo-1
