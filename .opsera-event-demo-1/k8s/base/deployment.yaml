# ════════════════════════════════════════════════════════════════════════════
# DEPLOYMENT - event-demo-1
# Generated by Opsera Code-to-Cloud Enterprise v0.903
# ════════════════════════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-demo-1
  labels:
    app: event-demo-1
    tenant: opsera
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-demo-1
  template:
    metadata:
      labels:
        app: event-demo-1
        tenant: opsera
    spec:
      # RULE 81: imagePullSecrets REQUIRED for private ECR registries
      imagePullSecrets:
        - name: ecr-registry-secret
      containers:
        - name: event-demo-1
          image: event-demo-1:latest
          # RULE 80: imagePullPolicy: Always is REQUIRED for Argo Rollouts
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          # Startup probe for slow-starting containers
          startupProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          # Liveness probe
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          # Readiness probe
          readinessProbe:
            httpGet:
              path: /ready
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 101
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 101
