# ════════════════════════════════════════════════════════════════════════════
# OPSERA CONFIGURATION - Single Source of Truth
# Generated by Opsera Code-to-Cloud Enterprise v0.903
# RULE 60: This file contains ALL deployment configuration
# ════════════════════════════════════════════════════════════════════════════

version: "0.903"
generated: "2026-02-02"

# Application Settings
app:
  name: event-demo-1
  runtime: nodejs
  framework: vite
  port: 80

# Tenant Settings
tenant:
  name: opsera
  # Naming convention: {tenant}-{app}-{env}
  namespace_pattern: "opsera-event-demo-1-{env}"

# Cloud Provider Settings
cloud:
  provider: aws
  region: us-west-2
  region_short: usw2

# Cluster Configuration
clusters:
  # RULE 70: Hub clusters follow argocd-{region} naming
  hub:
    name: argocd-usw2
    purpose: ArgoCD control plane
  # RULE 71: Spoke clusters follow {tenant}-{region}-{prod/np} naming
  spoke:
    name: opsera-usw2-np
    purpose: Application workloads

# Container Registry
registry:
  type: ecr
  # RULE 101: ECR repos use {tenant}/{app} pattern
  repository: opsera/event-demo-1

# Domain Configuration
dns:
  # RULE 64: Use agent.opsera.dev (singular, not agents)
  base_domain: agent.opsera.dev
  # Pattern: {tenant}-{app}-{env}.{base_domain}
  subdomain_pattern: "opsera-event-demo-1-{env}.agent.opsera.dev"

# Environment Configuration
environments:
  dev:
    replicas: 1
    strategy: rolling
    auto_promote: false
    timeout: 300  # 5 minutes
  qa:
    replicas: 2
    strategy: canary
    auto_promote: false
    timeout: 600  # 10 minutes (RULE 43)
  staging:
    replicas: 3
    strategy: blue-green
    auto_promote: false
    timeout: 600  # 10 minutes

# Security Scanning (Quick Setup defaults)
security:
  gitleaks:
    enabled: true
    mode: warn  # non-blocking
  grype:
    enabled: true
    mode: warn  # non-blocking
    severity_threshold: critical

# Optional Integrations
integrations:
  sonarqube:
    enabled: false
  slack:
    enabled: false
  jira:
    enabled: false

# GitHub Secrets Required
secrets_required:
  - AWS_ACCESS_KEY_ID
  - AWS_SECRET_ACCESS_KEY
  - AWS_ACCOUNT_ID
  - GH_PAT
