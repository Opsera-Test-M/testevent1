# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - DEV Environment
# Generated by Opsera Code-to-Cloud Enterprise v0.903
# RULE 59: Use destination.name for spoke cluster (hub-spoke architecture)
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: event-demo-1-dev
  namespace: argocd
  labels:
    app: event-demo-1
    environment: dev
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    # RULE 58: Repository URL must be resolved (no placeholders)
    repoURL: PLACEHOLDER_REPO_URL
    targetRevision: HEAD
    path: .opsera-event-demo-1/k8s/overlays/dev

  destination:
    # RULE 59: CRITICAL - Use 'name' for spoke cluster, NOT 'server'
    # This ensures deployment goes to spoke cluster, not hub
    name: PLACEHOLDER_SPOKE_CLUSTER
    namespace: opsera-event-demo-1-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
