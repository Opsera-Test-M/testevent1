# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - STAGING Environment
# Generated by Opsera Code-to-Cloud Enterprise v0.903 (GitOps Mode)
# RULE 59: Use destination.name for spoke cluster (hub-spoke architecture)
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: event-demo-1-staging
  namespace: argocd
  labels:
    app: event-demo-1
    environment: staging
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/Opsera-Test-M/testevent1.git
    targetRevision: HEAD
    path: .opsera-event-demo-1/k8s/overlays/staging

  destination:
    # RULE 59: Use 'name' for spoke cluster in hub-spoke architecture
    name: opsera-usw2-np
    namespace: opsera-event-demo-1-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
