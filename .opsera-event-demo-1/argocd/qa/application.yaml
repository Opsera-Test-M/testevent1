# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - QA Environment
# Generated by Opsera Code-to-Cloud Enterprise v0.903
# Deployment Strategy: Canary (10% → 30% → 60% → 100%)
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: event-demo-1-qa
  namespace: argocd
  labels:
    app: event-demo-1
    environment: qa
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: PLACEHOLDER_REPO_URL
    targetRevision: HEAD
    path: .opsera-event-demo-1/k8s/overlays/qa

  destination:
    # RULE 59: Use spoke cluster name
    name: PLACEHOLDER_SPOKE_CLUSTER
    namespace: opsera-event-demo-1-qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
