# =============================================================================
# 🐦 OPSERA DevOps Copilot - Kustomize Base Configuration
# Skill Version: 4.0.0
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: play1
  app.kubernetes.io/managed-by: opsera

# Note: Image transformation is handled by overlays (dev/staging/prod)
# Each overlay sets the specific image tag via the images section
